# Project-Market-Watch Status  (read by the command center)

> Our standard reporting file. The command center reads this to **track, coordinate, and sync**
> this project (`/dashboard`, `/align`). Keep it current as you work; right-size against the
> ~8 to 10 hrs/week total budget.

**Last synced:** 2026-08-31

## Tracking
- **Blocked:** nothing. Hosting decided 2026-08-30 (Ham: public repo plus GitHub Pages): the repo is https://github.com/thapras/market-watch, the page is https://thapras.github.io/market-watch/, the nightly Action is enabled and its first run was triggered by hand the same evening. The state and change log were reset before the push, so the three-close rule, the ninety-day log and the surprise index count from that first run.
- **Now:** v4.1, the visual pass, delivered 2026-08-30 on Ham's "suggest improvements to the visualization" then "proceed". The review measured the live page instead of eyeballing it (25,400 px tall on desktop, 49,400 on a phone; eight tables clipping their READ column by 19 to 257 px behind invisible overlay scrollbars; the phone showing only the first column of the tables that carry the reads) and found seven improvements; all shipped in one session, in the review's order. Defects: names and reads wrap, long text in numeric columns wraps, a table wider than its card fades at the right edge with a "scroll for more" hint; the scorecard, smart money, crowd and divergence tables become cards under 640 px. Visuals, every one drawn by the loader from `latest.json` with no library and using the page's own thresholds: the ranking as a map (conditions across, price up, the five read zones shaded, points fanned when they share a spot, labels that avoid points, a week-ago tail once the score history exists) plus a rule-based ranking read built from the read chips (the sample paragraph is gone); twelve-month trails behind the four composite dots (the fetcher now keeps thirteen month ends); inline encodings on the columns the copy says to scan (scorecard z on a plus or minus 3 axis with beyond 2 shaded, one-year and three-year percentiles with the 20 and 80 extremes shaded, breadth bars with the 35 and 60 thrust marks in the sector and theme tables); a week strip above the calendar (one column per day, the session's options-implied move as a bar against the normal session, tier-1 in the accent, closures hatched, today outlined) and one bar per release in the event study (median, worst tenth, the implied marker); section 2 gains the Treasury curve now against a month and a year ago (four tenors added to the FRED pull) and the fed funds path from twelve futures contracts with FOMC months ringed and the effective rate as the anchor; the rotation map's labels try right, left, above and below and avoid other points, with leader lines; the alert feed sorts state changes first and collapses watch and exit cards to their header; the sample reads in sections 1 to 3 are dashed and say "not from data". 51 unit tests (three added for the strip symbols, the curve and the path blocks). Verified with scripted Chrome screenshots at 1280 px light and dark and 390 px: no JavaScript errors, no ellipsis, no dashes, no table clipping its read at desktop width. Not yet committed or pushed: Ham reviews the diff first (the working tree holds `index.html`, five fetcher files, the tests, README, CLAUDE.md and these notes).
- **v4 (2026-08-30):** v4, the calendar, event studies and briefs, delivered 2026-08-30 on Ham's "continue v4". Section 5 is live and keyless: 34 events over ten weeks from Forex Factory's public JSON (the current week's times, consensus and prior; it carries no actuals), the Fed's FOMC calendar and historical pages (2011 on, read once into the state), the ECB and Bank of Japan meeting pages, the BLS release archives and schedules (payrolls, CPI), TreasuryDirect's announced auctions, and rules for the usual slots (ISM first and third business day, payrolls three Fridays after the reference week, the QRA on the Wednesday nearest the first of the month, checked against the BLS archive since 2015) and the mechanical dates (quarter end, corporate tax dates, quad witching, the Fed blackout and minutes from the meeting dates, US holidays); rule rows say "usual slot, to confirm" until a feed confirms them. The tabs and the tier-1 toggle work. Every row carries the three columns: rule-based by default (consensus and prior, the event study, the options bar, positioning from section 6), replaced by the Sunday brief from `data/briefs.json` (the `/brief` skill and `python3 -m fetch.brief` print the inputs and write the skeleton; reviewed entries carry their date). Event studies per release since 2011 from the BLS and Fed dates (payrolls n=187: the S&P moves 0.6% at the median on the day, 1.7% in the worst tenth; CPI 0.5%; FOMC n=128, 0.6%). Options-implied moves per session from the Cboe SPX chain (forward variance between consecutive daily expiries): payrolls ±0.8%, CPI ±0.9%, the September FOMC ±1.0%, a normal session ±0.6%. The in-house US surprise index logs Forex Factory consensus before each release and reads the first print from FRED after it (nine series, prior scales documented until twelve are logged); it scores once six have resolved, so about two months from the first nightly run. The calendar effects "Next" column is computed. The change log gains tier-1 events inside five days (notable), tier-2 (FYI, four at most) and tier-1 surprises beyond one sigma (must read). 48 unit tests; the nightly run took 70 seconds cached. Out of scope by design: earnings dates (no keyless feed), euro area and China surprise indices (no free consensus and actual pair), Finnhub and FMP (keys).
- **v3 (2026-08-30):** positioning and sentiment, delivered on Ham's second "Continue". Section 6 is live and rule-based: smart money against the crowd for nine markets from CFTC positioning (legacy commercials and non-commercials, TFF asset managers and leveraged funds, via the Socrata API), DIX and gamma exposure, Cboe put/call ratios (a year seeded, then logged daily), the AAII survey (logged weekly, scored once 26 weeks exist), FINRA margin debt and the crypto fear and greed index; divergence scores with the two-week alert rule and a "what happened before" column from three years of weekly crossings; the divergence dots; the Crowd regime chip; the ranking's P pillar for the eight markets with futures; the systematic flows table (CTA trend signals with since and flip level, vol-control allocation, risk parity leverage, dealer gamma from GEX, pension rebalancing). 790 cells; the sentiment cards' rows without a free feed (fund manager cash, insiders, Fear and Greed, Google Trends, Investors Intelligence, NAAIM, OCC, retail purchases, leveraged ETF flows, funding rates) stay sample and say so. Day-one reading worth Ham's attention: four of nine markets sit beyond the 1.5 line (10Y Treasuries +2.8, dollar -2.9, bitcoin -2.6, copper -3.7) and the three-year record of those crossings is poor (gold's contrarian-bearish crossings were followed by a +7.9% median), so the 1.5 line and the single-input construction are the first things to tune from the log.
- **v2 (2026-08-30):** the detector and regime, delivered on Ham's "Continue" after the v1.1 recap. Live and rule-based: the four regime composites (three-year z-scores of the inputs with a free feed; the explainer on the page lists what is in and what has no feed), the regime flags and a generated regime line, the Investment Clock position with a six-month trail, the rotation detector for fifteen themes plus the cash card (relative strength on vol-adjusted returns, the eight-week seasonal window over twenty years, a macro driver per theme, breadth thrust where a free holdings file exists: the SPDR funds; flows are shown as not scored, no free share-count feed), states with the three-close rule and a first-run that dates states from the relative strength cross, backtests per signal count since 2011 on the three conditions with history (evidence bar n of 20 and t of 2 gates Confirmed), the alert feed built from state, the rotation map with axes that scale to the data, the twenty-year seasonality table with hit rates on hover, the ranking's conditions score (pillars L, C, Y, S live; V for three markets; F and P not scored, shown as such), the read column and a re-ranked table, the state store (`data/state.json`) and the ninety-day change log (`data/changes.json`) behind the What changed strip, and `fetch/notify.py` for the digest and push (ntfy, Telegram or email, only when a channel secret is set and only when a must-read exists). 705 live readings, 128 placeholders; a fully cached run takes 43 seconds, a cold one about five minutes (571 member tickers for breadth). 41 unit tests. Betas, quadrant preferences and thresholds are starting points, documented in the code, to be tuned from the log after three months.
- **v1.1 (2026-08-30):** the free-feed follow-ups on top of v1: eight more sources, all keyless and standard library (BIS policy rates, EIA weekly stocks and monthly rig count, Cboe COR3M, Treasury FiscalData, BoJ M2, multpl, the ICI weekly release, the Cleveland Fed nowcast file). Live now: all six policy rates with last move and a rule-based stance (direction of the last move if within twelve months, else on hold); crude and SPR stocks; oil rigs (monthly); uranium via the Sprott physical trust as a labelled spot proxy; implied correlation; GDPNow and the Cleveland core CPI nowcast; CFNAI and payrolls (two rows added to the growth pulse, marked as proposals in the recap); bills share of marketable debt; the fiscal impulse; trailing P/E and the equity risk premium; money market fund assets and the weekly change. The M2 aggregate is US plus euro area plus Japan in dollars and is labelled Big-3, because China's M2 has no free monthly feed. 597 live readings, 236 placeholders. Draft "Read" narrative beside live numbers is now muted as sample until v2 writes reads from rules. v1 detail follows.
- **v1 (2026-08-30):** `fetch/` is a nightly Python script, standard library only, no API keys: FRED (fredgraph CSV), Yahoo Finance (chart endpoint) and the ECB data portal, 113 series, about two minutes. It writes `data/latest.json` (data contract in README.md). `index.html` reads it and fills 542 readings: the regime strip, the liquidity and rates tiles, the cross-asset scorecard, currencies, ratios, factors, precious metals, energy, industrial metals and agriculture, equity indices, sectors, themes, global 10-year yields, the Fed and ECB policy rates, claims and the Sahm rule, and the price side of the twelve-month ranking (five rules, tags, 12-1 momentum). Every live reading carries its source and date; the 284 placeholders that remain are tagged sample and shown in grey; a reading older than its refresh interval is greyed and dated (Yahoo's ^VIX3M, CSI 300 and SET feeds stopped on 17 Jul, which the page now shows rather than hides). A feed that fails carries forward its last good value marked stale. GitHub Actions workflow runs at 05:30 Bangkok and commits the file. Unit tests cover the five price rules and tags. Verified end to end locally in light and dark.
- **Next:** (0) Ham reviews the v4.1 diff and says commit and push; the nightly Action then serves the new page and data (the four tenors and the twelve contracts come from FRED and Yahoo, both already in the pipeline). (0) Ham reviews v4 on the page: the tier table and the row names in `fetch/calendar.py` (ROWS), the rule-based Stronger and Weaker lines (DRIVER_TEXT), the surprise prior scales (SURPRISE), and whether the implied-move bar (the median of the next ten sessions) is the right one. (1) The first Sunday brief: `/brief` in this folder, twenty minutes, writes `data/briefs.json`; until then every row says rule-based. (2) Ham reviews v3: the divergence construction (`fetch/positioning.py`, DIV_MARKETS and the sign conventions), the 1.5 line, and whether the 10Y row's "smart money buying" read is the basis trade rather than sentiment; and v2 (the regime line and chips, the alert cards, the ranking's conditions score; the betas and quadrant preferences in `fetch/conditions.py`). (3) Follow-ups, each small: a free ETF share-count feed for the flow condition (issuer pages, to probe), holdings parsers for the non-SPDR themes (VanEck, Global X, iShares, First Trust), the relative-strength rank-jump clause, a channel secret so the digest can go out, a keyless earnings-date probe (Yahoo's calendar needs a session crumb; the issuer IR pages are the fallback), the event study split by sign of surprise once the log holds 24 releases. (4) Repo and host when Ham decides, then the first Action run; the log, the three-close rule and the surprise index only start counting from that first nightly run. Still without a free feed after the probes: China M2 (NBS blocks non-browser clients, DBnomics mirror months behind), PBoC balance sheet, PMIs and ISM actuals, Korea 20-day exports, euro area and China surprise indices, Baltic Dry, gold ETF and central bank holdings, LME stocks, OPEC spare capacity, EUR basis, forward P/E and revisions, fund flows beyond money funds, earnings dates.
- **Done (recent):** 2026-08-31 build plan removed from the production page on Ham's request (nav link and footer updated; the content lives on as PLAN.md in the repo, converted to markdown, with the original markup in git history). 2026-08-31 phone readability fix on Ham's report: a probe found the What changed entries wrapping one word per line (a 48 px grid column), the Investment Clock and rotation map text at an effective 5 to 6 px, and every remaining read column pushed behind the fade; the strip entries now flow as sentences, the two maps keep full scale and pan sideways, and the card layout extends to all nineteen read-carrying tables including the calendar (relabelled on every tab redraw). 2026-08-30 v4.1 visual pass (above); tooling catch: the Playwright MCP screenshots time out on this page because of `scroll-behavior:smooth` under a 5 second cap, and Chrome's full-page capture wraps past 16,384 px, so the check now runs through a scripted Chrome (noted in CLAUDE.md). 2026-08-30 v4 calendar, event studies and briefs (above); probes: Forex Factory's JSON exists for this week only (nextweek and lastweek 404) and carries no actuals, so actuals come from FRED; the BLS archive pages list every release since 2008 plus the pre-registered schedule; the Fed's calendar page covers 2021 to 2027 and the historical pages fill 2011 to 2020; TreasuryDirect's upcoming-auctions JSON is open; the Cboe SPX chain (12.5 MB) carries per-option implied vols with daily expiries about six weeks out; FRED's release calendar is paginated and noisy, not used. Review catches before it shipped: the payroll rule moved Good Friday releases that the BLS actually publishes, and missed the New Year slip (both fixed against the archive: only shutdown delays differ since 2015); implied moves were attaching to Saturday and holiday rows (now only releases, meetings and auctions); consensus items followed the feed's order instead of the catalog's. 2026-08-30 v3 positioning and sentiment, probes closed: CNN fear and greed (418 to bots), NAAIM (JavaScript-rendered), Cboe CSV listings (403; per-day JSON open), CFTC annual zips (19 MB each, Socrata used instead). 2026-08-30 v2 detector and regime; review catches before it shipped: a linear-scan lookup in the backtest that would have cost minutes per night (now bisect), a timestamp slice that broke the log merge, composites dropping monthly inputs too early at weekly cadence, the map's fixed axes pinning themes like gold miners to the corners, first-run states all dated today, and label pile-ups on the map. 2026-08-30 v1.1 free-feed follow-ups; probes closed the door on EIA's CSV downloads (signed CloudFront redirects, so the HTML history tables are read instead), OFR's money fund API (needs a token now), the NBS API (403 for non-browser clients) and Baker Hughes (HTTP/2 reset, xlsb only). Review fixes: BIS files carry NaN on weekends, which broke two rows until the cleaner dropped NaN; the BIS China series is the one-year LPR, so the row is labelled that way. 2026-08-30 v0 draft through v0.6 (professional layer, twelve-month ranking, chart redraw, price-confirmation layer, What changed strip, wrapped labels), then v1: data sources probed (FRED behind Akamai rejects browser-imitating User-Agents, plain client UA passes; Stooq is behind a JavaScript wall; Japan and China M2 on FRED ended 2017 to 2019; EIA is not mirrored on FRED), fetcher and compute with tests, renderer, page binding (755 cells, 35 tiles, 18 strip items, 18 ranking rows), freshness and stale rules, workflow, README. Review fixes: "Failed breakout" fired on ten of seventeen rows with a 20-day level (now the 55-day level); rule B skipped for the two bond rows; date field collision that blanked the strip deltas.

## Coordination (cross-project)
- **Depends on:** GitHub (free) for the nightly Action and hosting. The Yahoo chart endpoint has no service guarantee; the page tolerates gaps (grey, dated) but a permanent block would need a second price source.
- **Blocking / needed by:** none.
- **Escalate to command center:** (1) Host: decided and done 2026-08-30, public repo plus GitHub Pages (see Blocked). Commits from this machine use a GitHub noreply address, set repo-locally, so the personal email stays off the public log. (2) Budget: v1 to v4 all shipped on 2026-08-30 in one long day of sessions, so the "nine to twelve weekends" estimate no longer applies; what remains is the weekly loop (twenty minutes on Sunday for the brief, a look at the alert log after three months to tune thresholds), which fits inside the 8 to 10 hrs/week alongside the iOS app and the SaaS. (3) Push channel: none set yet; adding an NTFY_TOPIC secret to the repo turns on must-read pushes.
